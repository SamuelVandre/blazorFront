@page "/"

<PageTitle>Home</PageTitle>
<style>
    
</style>

@*

git init  # Inicializa o repositório Git, caso ainda não tenha feito
git add .  # Adiciona todos os arquivos ao staging
git commit -m "Primeiro commit"  # Faz o commit dos arquivos
git remote add origin https://github.com/SEU_USUARIO/SEU_REPOSITORIO.git  # Adiciona o repositório remoto
git branch -M main  # Renomeia a branch principal para 'main'
git push -u origin main  # Envia os arquivos para o repositório remoto

*@